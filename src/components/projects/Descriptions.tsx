import { useMediaQuery } from "@material-ui/core";
import { motion, } from "framer-motion";


//list of projects desciption
const descriptions = [

]

export default function Descriptions(){

    return(
        <motion.div></motion.div>
    )
}