import { makeStyles } from "@material-ui/core";

const useShareOnMediaStyle = makeStyles(theme => ({
    container : {
        zIndex: 3,
        position : "absolute",
        left : "30px",
        bottom :"50px",
    }
}))

export default useShareOnMediaStyle;