import { makeStyles } from "@material-ui/core";

const useCommentsStyle = makeStyles(theme => ({
    root : {
        position : 'relative',
        height : '60vh',
        // border :'2px solid red'
    },
}));

export default useCommentsStyle;